angular.module('angularApp').directive('svgMapMain', ['$compile', function ($compile) {
    return {
        restrict: 'A',
        templateUrl: '/uploads/maps/2VR_G_1-01.svg',
        link: function (scope, element, attrs) {
            
        }
    }
}]);

angular.module('angularApp').directive('svgMapSite', ['$compile', function ($compile) {
    return {
        restrict: 'A',
        templateUrl: '/uploads/maps/sitemap.svg',
        link: function (scope, element, attrs) {
            
        }
    }
}]);

angular.module('angularApp').directive('svgMapOneVenture', ['$compile', function ($compile) {
    return {
        restrict: 'A',
        templateUrl: '/uploads/maps/SCIENCE.G.svg',
        link: function (scope, element, attrs) {
            
        }
    }
}]);

angular.module('angularApp').directive('svgMapEpsilon', ['$compile', function ($compile) {
    return {
        restrict: 'A',
        templateUrl: '/uploads/maps/EPSILON.G.svg',
        link: function (scope, element, attrs) {
            
        }
    }
}]);

angular.module('angularApp').directive('svgMapBenham', ['$compile', function ($compile) {
    return {
        restrict: 'A',
        templateUrl: '/uploads/maps/5BENHAM-01.svg',
        link: function (scope, element, attrs) {
            
        }
    }
}]);

angular.module('angularApp').directive('svgMapKenneth', ['$compile', function ($compile) {
    return {
        restrict: 'A',
        templateUrl: '/uploads/maps/KEN-DIB.svg',
        link: function (scope, element, attrs) {
            
        }
    }
}]);

angular.module('angularApp').directive('svgMapThreeVenture', ['$compile', function ($compile) {
    return {
        restrict: 'A',
        templateUrl: '/uploads/maps/3vr.svg',
        link: function (scope, element, attrs) {
            
        }
    }
}]);

angular.module('angularApp').directive('svgMapTransport', ['$compile', function ($compile) {
    return {
        restrict: 'A',
        templateUrl: '/uploads/maps/bus-map.svg',
        link: function (scope, element, attrs) {
            
        }
    }
}]);